# Project Rules & Preferences

- **Python usage constraint**: Avoid executing Python scripts unless strictly necessary. Always ask explicit user confirmation before attempting to run Python scripts or commands. Use native Node.js / shell tools instead wherever possible.
